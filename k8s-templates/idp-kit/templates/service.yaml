apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp-kit.fullname" . }}
  labels:
    {{- include "idp-kit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idp-kit.selectorLabels" . | nindent 4 }}
